#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @h: The integer to be computed
 * Return: The absolute value of
 */
int _abs(int h)
{
	if (h >= 0)
		return (h);
	else
		return (-h);
}
